# FitDiet 当前唯一 PRD

## 0. 文档定位

本文档是本项目当前唯一的产品需求文档（PRD）。

从本次改版开始，不再以“Web 首版 MVP 文档”作为主事实来源。所有产品判断、功能优先级、流程设计、数据约束、迁移边界与交付标准，统一以本文件为准。

本项目当前处于迁移阶段：

- 保留旧 Web MVP 继续可运行
- 新功能主战场转向移动端优先架构
- 后端同步迁移到 FastAPI 分层体系
- 迁移必须逐步进行，不能破坏旧 MVP 的现有可见流程

相关参考但非主事实来源：

- `README.md`：当前工程交接说明、启动方式、验证记录、剩余风险
- `UI_STYLE_PROMPT.md`：视觉风格参考
- `public/` 与 `server/app.py`：旧 Web MVP 的交互与视觉参考实现

---

## 1. 产品目标

### 1.1 一句话定位

一款面向健身用户的移动端优先饮食管理产品，帮助用户在增肌、减脂和维持阶段持续记录饮食与运动，理解每日执行结果，并通过清晰反馈与周期计划稳定推进目标。

### 1.2 当前产品目标

当前阶段的核心目标不是扩写旧 Web 功能，而是完成一套可逐步替代旧 MVP 的移动端优先产品基础能力：

- 能快速记录饮食
- 能记录运动消耗
- 能计算今日剩余热量
- 能让用户理解自己今天是否按计划执行
- 能在后续自然扩展到离线优先、可靠同步、长期计划管理

### 1.3 目标用户

核心用户为有一定健身基础、具备基础营养认知的普通用户：

- 理解热量、蛋白质、碳水、脂肪等概念
- 可能处于增肌、减脂或维持期
- 希望记录流程足够快，不愿被复杂表单打断
- 需要明确知道今天“还能吃多少”以及“执行得怎么样”
- 愿意通过周期计划与周报做复盘，而不是只做流水账记录

### 1.4 产品原则

- 移动端优先，桌面端为次要场景
- 记录速度优先于复杂功能堆叠
- 先打通核心闭环，再扩展复杂能力
- 饮食记录不能因为网络失败而完全阻塞
- AI 识别只能作为辅助，用户必须始终可编辑
- 不牺牲可维护性来换取短期堆功能

---

## 2. 当前架构方向

### 2.1 当前系统状态

项目当前由三部分组成：

1. 旧 Web MVP
   - 目录：`public/` + `server/app.py`
   - 作用：保留现有可运行版本、提供旧交互与视觉参考
   - 数据：SQLite

2. 新移动端
   - 目录：`mobile/`
   - 技术：React Native Expo、TypeScript、React Query、React Hook Form、Expo Router
   - 作用：未来主产品入口

3. 新后端
   - 目录：`backend/`
   - 技术：FastAPI、Pydantic、SQLAlchemy
   - 作用：承接移动端主业务能力

### 2.2 当前迁移结论

- 旧 Web MVP 继续保留，不删除，不作为新功能主战场
- 未来产品主路径必须优先落在 `mobile/ + backend/`
- 需要持续参考旧 Web 的可见流程与内容结构，但不再把“Web 响应式首页”视为目标产品形态
- 产品需求、设计判断、接口新增，应默认以移动端体验为第一优先级

### 2.3 不允许出现的偏差

- 不要把新需求继续大量堆回旧 Web MVP
- 不要重写旧 MVP 的现有用户可见流程，除非明确要求
- 不要让移动端和后端的数据结构长期分裂
- 不要在 UI 组件里直接写业务计算
- 不要将离线优先当作可后置很久的“锦上添花”能力

---

## 3. 当前产品范围

### 3.1 当前必须完成的能力

#### A. 计划与目标

- 用户可以创建当前阶段计划：减脂 / 增肌 / 维持
- 用户可以输入开始时间、结束时间、起始体重、目标体重、运动频率
- 系统自动计算每日目标热量
- 系统展示当前激活计划

#### B. 身体数据

- 用户可以记录当前体重
- 用户可逐步扩展记录胸围、腰围、臀围
- 系统基于身高、体重、年龄、性别计算 BMI、BMR、TDEE
- 体脂估算以 Deurenberg 公式为主，围度只能作为辅助参考

#### C. 饮食记录

- 用户可以搜索食物
- 用户可以选择食物并记录摄入
- 系统自动写入该条记录的热量和三大营养素
- 用户可以查看今日饮食记录
- 后续必须支持编辑和删除

#### D. 运动记录

- 用户可以记录运动项目、时长和消耗热量
- 系统把运动消耗回补到今日可用热量额度中
- 用户可以查看今日运动记录

#### E. 今日仪表盘

- 展示当前日期
- 展示当前阶段目标类型
- 展示今日目标摄入
- 展示今日已吃热量
- 展示今日运动消耗
- 展示今日剩余热量
- 展示完成度
- 后续补齐蛋白、碳水、脂肪明细与达成率

#### F. 个人信息与设置

- 用户可以查看与编辑基础资料
- 用户可以查看最近身体数据
- 用户可以调整目标相关信息
- 用户可以调整基础设置项

### 3.2 当前高优先级但可分阶段落地的能力

- 饮食记录编辑
- 饮食记录删除
- 今日记录列表完整展示
- 宏量营养素达标可视化
- 周报基础版
- 本地缓存当天数据
- 失败重试与未同步队列
- 自动同步与冲突最小化处理

### 3.3 明确延后能力

- 拍照识别食物
- 社区与内容系统
- 教练后台
- 大而全品牌食品库
- 月报和复杂分析面板
- 高级提醒系统
- 多端复杂协作

---

## 4. 产品信息架构

当前移动端一级导航结构：

1. 今天
2. 计划
3. 心情
4. 我的
5. 中央 `+` 记录入口

### 4.1 今天

作用：查看当日执行结果与下一步行动方向。

包含：

- 今日执行状态
- 当前计划类型
- 今日剩余热量
- 今日已吃 / 目标摄入
- 完成度
- 宏量营养素概览
- 今日建议
- 进入记录入口

### 4.2 计划

作用：管理当前阶段目标与周期。

包含：

- 当前目标类型
- 起始体重
- 目标体重
- 周期时间
- 每周运动频率
- 系统计算出的目标热量

### 4.3 心情

作用：预留轻量自我状态记录与行为反馈入口。

当前可先作为结构保留，不要求复杂实现。

后续可用于：

- 饮食执行主观感受
- 饥饿感 / 精力 / 恢复状态记录
- 周报补充解释

### 4.4 我的

作用：管理个人资料、身体信息、偏好设置。

包含：

- 基础资料
- 身高体重
- 围度数据
- 当前设置项
- 未来的账号与同步状态

### 4.5 记录中心

作用：快速新增一条“吃一点”或“动一下”。

包含：

- 吃一点
- 动一下

这是当前产品最重要的高频入口之一，必须保证：

- 打开快
- 输入少
- 提交反馈清晰
- 网络失败时仍可保留记录意图

---

## 5. 核心用户流程

### 5.1 首次使用流程

1. 用户进入应用
2. 填写基础资料：昵称、性别、年龄、身高、当前体重
3. 创建当前目标计划
4. 系统计算每日目标摄入
5. 跳转到“今天”页
6. 用户从“+”开始第一次饮食记录

成功标准：

- 用户能在较少步骤内完成建档
- 用户能立刻开始记录
- 用户能立刻看到“今天还能吃多少”

## 5.2 记一餐流程

目标：这是当前全产品最关键流程。

标准流程应为：

1. 用户点击中央 `+`
2. 进入记录中心
3. 选择“吃一点”
4. 搜索食物
5. 选择食物
6. 输入数量
7. 确认单位
8. 提交记录
9. 返回今天页或记录结果页
10. 今天页即时刷新剩余热量与完成度

当前要求：

- 食物记录字段必须完整
- 单位必须归一化
- 营养计算应在服务层完成，不应散落在 UI
- 失败时需要给出用户可理解的反馈
- 后续必须支持离线暂存与自动重试

### 当前实现与目标之间的差距

当前移动端虽已具备基础“吃一点”入口，但仍未完全达到目标流程：

- 记录确认链路仍偏短
- 编辑 / 删除能力未形成闭环
- 离线补偿尚未补齐
- 宏量营养素的展示与接口返回尚未完全打通

这部分是后续最高优先级改进区域。

## 5.3 记运动流程

1. 用户点击中央 `+`
2. 进入记录中心
3. 选择“动一下”
4. 输入运动项目、时长、消耗
5. 提交
6. 今天页刷新今日剩余热量

原则：

- 输入应比记一餐更轻
- 用户不必被复杂运动分类阻塞
- 允许后续补充更精细的消耗估算逻辑

## 5.4 每日查看流程

1. 用户打开应用进入“今天”
2. 立即看到今日目标与剩余额度
3. 如未记录，页面给出明确引导去记录
4. 如已记录，页面能帮助判断下一餐是否需要调整
5. 用户可反复从“今天”跳转到记录入口

该流程的目标不是“展示很多图表”，而是快速支持当下决策。

## 5.5 周期执行流程

1. 用户设定一个阶段计划
2. 每天记录饮食与运动
3. 定期记录体重 / 围度
4. 系统累计形成执行数据
5. 周报输出本周表现、偏差与建议
6. 用户决定继续、调整或切换目标阶段

---

## 6. 数据模型约束

### 6.1 食物记录结构

食物记录必须统一为以下字段：

```text
food_name
calories
protein
carbs
fat
quantity
unit
timestamp
```

### 6.2 单位规则

内部单位只允许：

```text
g
ml
serving
```

要求：

- 任何旧数据或旧逻辑中的 `piece` 等单位，都必须迁移或映射为允许单位
- 不允许在新计算逻辑中混用多套单位语义
- 服务端和客户端必须对单位含义保持一致

### 6.3 营养计算规则

- 营养值计算属于服务层职责
- UI 层只负责输入、展示和状态反馈
- 相同食物在相同输入条件下应得到稳定一致的结果
- 不允许在多个页面中重复实现不同版本的营养计算

### 6.4 身体算法规则

#### BMI

```text
BMI = weight_kg / (height_m * height_m)
```

#### BMR

使用 Mifflin-St Jeor：

- 男：`10w + 6.25h - 5a + 5`
- 女：`10w + 6.25h - 5a - 161`

#### TDEE

```text
TDEE = BMR * activity_factor
```

#### 体脂率估算

优先使用 Deurenberg 公式。

原则：

- 可以给趋势参考
- 不可伪装成高精度医学结果
- 围度估算只能作为辅助，不可替代主要公式结论

### 6.5 目标热量规则

用户不直接手工决定每天热量差值，而是由系统根据周期自动计算：

```text
daily_delta = 目标体重变化kg * 7700 / 周期天数
每日目标摄入 = TDEE + daily_delta
```

其中：

- 减脂：`daily_delta` 为负
- 增肌：`daily_delta` 为正
- 维持：`daily_delta` 接近 0

### 6.6 今日剩余热量规则

```text
remaining = target_intake + exercise_burned - food_consumed
```

该值是今天页的核心指标之一。

---

## 7. API 设计原则

### 7.1 总原则

所有 API 必须：

- 使用明确 typed request / response model
- 使用 Pydantic 做输入校验
- 返回一致的 envelope 结构
- 错误信息可被客户端稳定消费

### 7.2 分层要求

FastAPI 目标结构：

- `routers/`
- `services/`
- `repositories/`
- `schemas/`
- `models/`

要求：

- Router 保持薄
- 业务逻辑放在 service
- 数据查询与写入放在 repository
- schema 明确区分输入与输出模型

### 7.3 客户端调用要求

- UI 组件不能直接 `fetch`
- 必须走统一 API client 与 typed service functions
- 所有 mutation 必须考虑 optimistic update、retry 和错误反馈

### 7.4 当前重点 API 方向

当前产品重点依赖以下接口域：

- dashboard
- foods search
- food logs
- exercise logs
- plans
- profile / body metrics

这些接口后续应继续向“移动端今天页与记录流程”优先靠拢，而不是围绕旧 Web 页面组织。

---

## 8. 离线优先与同步要求

这是当前目标架构的必要要求，不是可有可无的增强项。

### 8.1 必须达到的离线能力

- 食物记录不能因为短时断网而完全失败
- 今日数据要支持本地持久化
- 未同步记录要进入本地队列
- 恢复网络后自动重试同步
- 用户能理解一条记录当前是“已同步”还是“待同步”

### 8.2 状态设计要求

所有异步状态必须体现：

- loading
- error
- retry behavior

### 8.3 推荐同步策略

- 先本地创建记录并立即更新 UI
- 后台尝试同步
- 同步成功后更新状态
- 同步失败则保留待同步标记并自动重试

### 8.4 风险提醒

如果没有离线队列和重试机制，这个产品在真实移动网络环境中会显著损伤使用体验，尤其会直接打断最核心的“记一餐”流程。

---

## 9. 视觉与交互方向

### 9.1 当前视觉方向

移动端视觉必须回归旧版的黑白拟物风格，而不是彩色现代运营风界面。

关键词：

- 黑 / 白 / 灰阶
- iOS 风格
- 毛玻璃
- 圆角卡片
- 拟物阴影
- 高可读性
- 低密度布局

### 9.2 交互原则

- 高优先操作必须明显可点
- 触控区域不小于 44px
- 不要使用复杂层级打断快速记录
- 页面应围绕一件事展开，不做大而杂的巨屏
- 主要按钮视觉层级必须强

### 9.3 移动端页面要求

所有页面必须支持：

- dark mode
- safe area
- keyboard avoidance
- responsive layout

### 9.4 视觉参考

参考来源：

- `UI_STYLE_PROMPT.md`
- 旧版 `public/index.html`
- 旧版 `public/styles.css`
- 旧版 `public/app.js`

注意：这些是视觉和内容结构参考，不再是产品事实来源。

---

## 10. 非功能要求

### 10.1 性能

优先级：

- 快速启动
- 快速进入记录入口
- 平滑滚动
- 低内存使用
- 避免不必要请求

### 10.2 代码质量

- TypeScript strict 必须保持开启
- 禁止 `any`
- 禁止重复 API 逻辑
- 禁止把业务计算散落在 UI
- 优先小而清晰的函数和类型

### 10.3 安全与错误处理

- 不暴露原始后端错误给用户
- 不记录 token、密码或健康隐私数据
- 不把密钥放在前端代码中
- 所有异步操作都必须有可理解错误反馈

---

## 11. 测试与验收标准

### 11.1 关键测试流

移动端关键流程必须逐步补齐测试：

- login
- add meal
- edit meal
- sync records

工具方向：

- Jest
- Detox

### 11.2 当前阶段最低验收标准

每次涉及核心流程改动时，至少应验证：

1. 今天页可正常加载
2. 食物搜索可正常返回结果
3. 能成功新增一条饮食记录
4. 能成功新增一条运动记录
5. 今天页剩余热量会随记录变化
6. 错误状态对用户可见

### 11.3 当前已知待补齐点

- 宏量营养素明细链路未完全打通
- 饮食记录编辑 / 删除未形成完整闭环
- 离线同步链路尚未完成
- 生产 PostgreSQL 迁移未完成
- 周报仍未按当前移动端方向完整落地

---

## 12. 当前优先级排序

### P0：必须优先完成

1. 记一餐流程稳定化
2. 记运动流程稳定化
3. 今天页数据闭环
4. 数据合同统一
5. 单位归一化

### P1：紧接着完成

1. 饮食记录编辑 / 删除
2. 今日记录列表完整展示
3. 宏量营养素明细
4. 本地持久化今日数据
5. 待同步队列与自动重试

### P2：在核心闭环稳定后推进

1. 周报基础版
2. 更完整的身体数据趋势
3. 心情页的有效化
4. 更细的计划调整能力

---

## 13. 当前项目风险

### 13.1 文档风险

如果团队继续同时参考旧 Web 文档和迁移说明，而没有唯一 PRD，会持续出现方向分裂。

本次重写后，应以本文件作为唯一 PRD。

### 13.2 架构风险

当前三套入口并存，容易导致：

- 改错位置
- 接口与页面脱节
- 重复实现同一能力

### 13.3 产品风险

如果优先做复杂页面或报表，而不是先把“记一餐 + 今天页 + 离线补偿”打稳，产品会继续停留在展示优先、使用闭环不足的状态。

### 13.4 数据风险

旧 MVP 存在 `piece` 单位等历史数据语义，若不尽快收敛，会持续污染新系统的计算与接口一致性。

---

## 14. 明确的执行边界

### 14.1 可以做

- 继续增强 `mobile/` 主路径体验
- 继续扩展 `backend/` 的分层能力
- 逐步把旧 Web 的有效内容迁移到移动端
- 为移动端体验补齐离线、同步、校验、测试

### 14.2 不可以做

- 删除旧 Web MVP
- 把旧 MVP 大改成另一套新产品
- 把复杂业务逻辑散落到 UI
- 继续把新增主功能堆回 `public/` + `server/app.py`
- 忽视离线优先要求
- 让 API 合同长期不统一

---

## 15. 结论

从现在开始，本项目不再是“一个 Web 饮食管理 MVP”，而是“一个正在从旧 Web MVP 迁移到移动端优先架构的饮食管理产品”。

产品工作的第一优先级不是扩页面数量，而是打稳以下核心链路：

- 计划建立
- 记一餐
- 记运动
- 今天页反馈
- 离线补偿与同步

只要这条主链路没有稳定，其他扩展能力都应让位。