# FitDiet UI 设计迁移提示词

请将现有应用 UI 迁移为以下视觉风格：

## 核心风格

iOS 应用 UI 设计，苹果美学，极简黑白界面，圆角，柔和但明显的立体阴影，高质量图标感，洁净排版，背景模糊 / 毛玻璃效应，SF Pro 字体，高保真。

## 颜色系统

- 整体只使用黑、白、灰阶。
- 避免蓝、绿、橙、紫等彩色强调色。
- 主文字使用接近纯黑：`#0a0a0a`。
- 次级文字使用中性灰：`#666666` / `#8e8e93`。
- 背景使用白到浅灰渐变：`#ffffff` → `#f4f4f6` → `#d9d9de`。
- 主要行动按钮使用黑色石墨渐变：`#2a2a2c` → `#050505`。
- 卡片使用白色 / 浅灰玻璃质感：`rgba(255,255,255,.72)`，配合 `backdrop-filter: blur(...)`。

## 立体效果

按钮和卡片必须有明显立体感：

- 外阴影：右下深色阴影，左上白色高光。
- 内阴影：用于输入框、进度槽、图标容器，形成按压或凹陷效果。
- 卡片边框使用半透明白色边框，增强玻璃层次。
- 主要卡片可以使用黑色渐变形成高对比视觉焦点。

推荐阴影：

```css
box-shadow:
  18px 22px 54px rgba(0, 0, 0, 0.18),
  -10px -10px 30px rgba(255, 255, 255, 0.9),
  inset 0 1px 0 rgba(255, 255, 255, 0.96);
```

推荐按压 / 凹陷效果：

```css
box-shadow:
  inset 8px 8px 18px rgba(0, 0, 0, 0.12),
  inset -8px -8px 18px rgba(255, 255, 255, 0.92);
```

## 布局规则

- 移动端优先，底部 Tab Bar 固定在底部，带毛玻璃和强立体阴影。
- 桌面端可以保留侧边栏，但也必须使用黑白毛玻璃和立体层级。
- 页面内容保持低密度：大标题、清晰分组、卡片间距充足。
- 所有可点击元素触控高度不少于 44px。
- 输入框、选择框使用大圆角和内阴影。

## 组件方向

- 主按钮：黑色凸起实体按钮，白色文字。
- 次按钮：白色 / 浅灰凸起按钮，黑色文字。
- 卡片：白色玻璃卡片，圆角 24px-30px，凸起阴影。
- 主数据卡：黑色渐变卡片，白色文字，作为页面视觉中心。
- 进度环 / 进度条：黑白灰表达，不使用彩色。
- Toast：黑色毛玻璃浮层。

## 排版

- 字体：`-apple-system, BlinkMacSystemFont, "SF Pro Display", "SF Pro Text", "Helvetica Neue", Arial, sans-serif`。
- 标题使用更大的字号和负字距，接近 iOS 大标题风格。
- 次要说明保持浅灰、短句、低视觉权重。

## 禁止事项

- 不要使用彩色品牌色作为主视觉。
- 不要使用硬边框、扁平 Material 风格、强网格感后台系统 UI。
- 不要牺牲可读性；黑色卡片只用于重点信息，不应大面积压暗界面。